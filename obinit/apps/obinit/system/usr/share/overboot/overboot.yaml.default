enabled: false
config_dir: "overboot.d"
layers:
  visible: true
  device: "/var/obdev.d"
  repository: "overboot"
  head: "root"
upper:
  type: "tmpfs"
  size: "50%"

# --- other upper types ---

# upper:
#  type: "persistent"

# upper:
#   type: "volatile"

# --- example durables ---

# durables:
#     - path: "/var/lib/mysql" # directory
#       copy_origin: true
#     - path: "/var/log/syslog" # file
#       copy_origin: true


