#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cmake

override_dh_auto_configure:
	dh_auto_configure -- \
		-DOB_USE_BLKID=ON \
		-DOB_BUILD_TESTS=OFF
	cp -v apps/obinit/deb/postinst debian/overboot-obinit.postinst
	cp -v apps/obinit/deb/postrm debian/overboot-obinit.postrm
